---
name: 金融营销材料合规审查
description: 对基金、资管、理财产品的营销宣传材料做发布前合规审查，输出可追溯到法条原文的问题清单与修改建议。适用于产品推介材料、产品说明书、公众号推文、路演 PPT、海报、短视频脚本等在对外发布前的自查，也可用于新规发布后对存量材料的批量重扫。触发词包括：合规审查、宣传材料审查、营销材料审查、推介材料检查、这份材料能不能发、有没有违规表述、审一下这个推文、基金宣传合规、存量材料重扫。
---

# 金融营销材料合规审查

对营销材料做**发布前事前审查**，定位是合规人员的第一道筛子，不是最终判定者。

审查依据是本地规则库（`references/rules/`），每条规则指向 `references/legal-index.md` 中已核实的法条原文。所有处理在本地完成，材料内容不出网。

## 边界声明

务必在输出报告时向用户明确以下三点，不得省略：

1. **本工具输出的是待复核清单，不是合规结论。** 最终判定权在合规人员。
2. **标记为「需人工研判」的项必须由人工确认。** 这类规则依赖上下文语义或外部数据（基金合同、管理人产品清单、官方风险等级），工具只能标记位置。
3. **未检出问题不等于材料合规。** 规则库覆盖范围有已知边界，见 `README.md`。

不要使用"审核通过""合规""可以发布"这类表述作为结论。

## 工作流

### 第一步：确认材料与审查上下文

读取待审材料。支持纯文本、Markdown。若用户提供 PDF、DOCX、PPTX，先用环境中可用的文档解析能力提取纯文本，再交给脚本。

在扫描前需确定四个上下文维度，它们决定适用哪些规则。**不要自行猜测，无法从材料中明确判断的要向用户确认。**

| 维度 | 取值 | 判断依据 |
|---|---|---|
| 产品类型 | `public_fund` 公募基金 / `private_fund` 私募基金 / `am_plan` 资管计划 / `wealth_mgmt` 理财产品 | 材料中的产品全称、"证券投资基金"字样、备案编码 |
| 受众范围 | `public` 面向不特定对象 / `specific` 面向特定对象 | 是否公开发布、有无合格投资者确认环节 |
| 材料载体 | `print` 平面 / `online` 线上 / `video` 视频 / `audio` 音频 | 用户说明的投放渠道 |
| 机构类型 | `fund_manager` 基金管理人 / `securities_firm` 证券公司 / `bank` 银行 / `wealth_subsidiary` 理财子公司 | 材料落款、用户说明 |

这四个维度不是可选项，直接影响法条引用是否正确。同一句话在公募和私募材料中依据的法条不同——例如"避险"一词，私募场景下明文禁用（中基协《私募投资基金募集行为管理办法》第二十四条第四项），公募场景下无现行依据（该词出自已废止的证监会公告〔2008〕2 号）。搞错产品类型会导致引用错误法条。

产品类型与受众范围通常能从材料判断；**材料载体与机构类型若无法确定，应向用户询问**。

四个维度中任一未提供时，带该维度限定的规则一律被跳过（宁可漏报也不在信息不足时误报），并计入 `stats.skipped_rules`，须在报告中说明哪些检查项因此未被覆盖。**产品类型尤其不可省略**：不提供它会使 39 条带产品限定的规则全部失效，扫描形同虚设。

### 第二步：运行确定性扫描

```bash
python3 {skill_dir}/scripts/scan_rules.py \
  --file <材料路径> \
  --product <产品类型> \
  --audience <受众范围> \
  --media <材料载体> \
  --institution <机构类型> \
  --json
```

未确定的维度省略对应参数即可。脚本零第三方依赖，兼容 Python 3.9。

输出为 JSON，`findings` 中每项包含：命中文本、行号、原文片段、法条依据（法规全称+条款号+条款原文）、处罚案例、修改建议、引擎说明。

#### 脚本失败时不得自行生成报告

**扫描脚本退出码非 0 时，向用户报错并停止，不要凭阅读材料自行写一份报告。** 没有扫描结果就没有法条依据与准确计数，这种报告看起来完整却无法举证，比不出报告危险。

| 退出码 | 含义 | 应对 |
|---|---|---|
| 2 | 文件不存在、读取失败、规则库加载失败 | 向用户说明具体错误，确认路径或规则库状态 |
| 3 | 材料内容为空或过短 | 多为 PDF/DOCX/PPTX 文本抽取失败，告知用户并确认原件可否正常解析 |

同样，若 `stats.pattern_errors` 非空，说明部分检查项已失效、本次结果不完整，此时也不得出具报告。

#### findings 明细可能被截断，计数不受影响

长材料中同一规则可能命中数十次，脚本默认每条规则最多输出 10 处明细，超出部分记入 `summary.findings_truncated`（按 rule_id 给出省略数）。**`summary` 系列计数始终基于全量命中**，报告中的数字照常引用即可。需要完整明细时加 `--max-per-rule 0`，但要注意输出体积。

`verdict` 字段有四种取值，**不同取值的处理方式不同**：

| verdict | 含义 | 你应该做什么 |
|---|---|---|
| `violation` | 命中即违规 | 直接列入报告 |
| `evidence_required` | 法条限定"未提供客观证据时"才违规 | 检查材料是否提供了数据出处，引擎已做初步判断，你需复核 |
| `manual_review` | 需上下文或外部数据判断 | 进入第三步做语义研判 |
| `advisory` | 无条款依据的建议 | 列入建议区，不得表述为违规 |

### 第三步：语义研判

确定性扫描只能覆盖能被规则表达的部分。以下情形规则库检不出，需要你阅读材料后判断：

- **整体印象误导**：逐句都不违规，但通篇只讲收益不讲风险，或用叙事方式暗示确定回报
- **变相承诺**：不含任何违禁词的收益保障暗示，如描述交易结构时暗示本金安全
- **风格漂移**：宣传标签与产品实际投资范围不符，如债券型基金宣传"高弹性成长机会"
- **专业术语未解释**：雪球结构、敲入敲出、指增、固收+、最大回撤等术语直接使用而未说明含义
- **数据内部矛盾**：同一材料中不同位置的数据、日期、口径互相冲突

对 `verdict` 为 `manual_review` 的每一项，结合上下文给出判断，并说明判断理由。**不要简单复述引擎输出**，要给出你的分析。

### 第四步：生成报告

报告按以下结构组织：

```
## 审查结论摘要
材料类型、审查上下文、发现总数与分级统计
规则库指纹与扫描日期（取 rule_set.fingerprint、scanned_at）
明确写出：本报告为待复核清单，不构成合规结论

## 需立即修改（violation，red 级）
## 需修改（violation，orange 级）
每项包含：
  原文引用（含位置）
  违反条款（法规全称 + 条款号 + 条款原文）
  处罚案例（如有）
  修改建议

## 需补充证据（evidence_required）
说明需要补充什么证据，而非要求删除

## 需人工研判（manual_review）
列出你的分析与建议，标明需合规人员确认

## 建议优化（advisory）
明确标注无条款依据，属行业惯例或呈现优化

## 尚未生效规则的提前排查
单独成节，标明规则生效日期，不与现行规则混列

## 本次未覆盖的检查项
说明因上下文信息不足被跳过的规则，以及规则库的已知边界
```

**每一项必须包含法条原文。** 这是本工具与通用文案检查工具的根本差异——合规人员需要据此向监管举证。不要用"违反相关规定"这类模糊表述。

法条原文**逐字复制** `legal_basis[].excerpt` 的内容，一个字都不要改。不得删减开头的主语、不得把"应当"简写为"应"、不得替换标点。举证材料中的条款引用与原文不符会直接影响其可用性。

对于 `basis` 为 `industry_practice` 的项，**必须明确标注"无现行条款依据"**，不得为其编造条款号。

#### 被审材料的原文引用同样不得改写

报告中「原文」一项取自 finding 的 `excerpt` 字段，或从材料对应行号处照抄，**不要凭阅读印象复述**。把"同类排名第一"写成"同类产品排名第一"这类偏差看似无害，但报告要发给业务部门作为整改依据，引用的句子在材料中检索不到会直接影响其可用性。

两种情况允许偏离逐字引用，但都需明示：截断引用时用省略号标出截断位置；表格类内容跨多行时可拼接，但要说明这是跨行摘录而非连续原文。

#### 报告中的数字一律取自扫描结果，不要自己数

手工统计几十条 findings 极易出错，且错误难以被发现。以下数字直接引用对应字段：

| 报告内容 | 取值字段 |
|---|---|
| 发现总数 | `summary.total` |
| 现行有效项数、未生效项数 | `summary.active_total`、`summary.pending_total` |
| 各判定类型数量 | `summary.by_verdict` |
| 判定与级别的交叉数量（如"橙级违规 14 项"） | `summary.by_verdict_severity` |
| 适用与未适用规则数 | `stats.rules_applied`、`stats.rules_skipped` |
| 未覆盖检查项的清单与原因 | `stats.skipped_rules`（含 rule_id、title、reason） |

#### 报告须标注规则库指纹

摘要中写明 `rule_set.fingerprint` 与 `scanned_at`。合规报告要存档备查，若干月后被追问某项认定的依据时，须能凭指纹取回当时的规则与法条原文重新复算。指纹覆盖全部规则文件与法条索引，任一处改动都会使其变化。

#### 扫描结果若含 pattern_errors，必须中止并报告

`stats.pattern_errors` 非空表示有正则模式无法编译，对应检查项已静默失效，**本次扫描结果不完整**。此时不得出具审查报告，应向用户说明哪条规则失效并要求修复规则库后重扫。

#### 数字容易出错的两个地方

- **未生效项已计入 `summary.total` 与 `by_verdict` 系列字段**。摘要中若单独提及未生效项，须写明"其中 N 项"而非"另有 N 项"，避免读者误以为需要相加。用 `active_total` 与 `pending_total` 表述最清晰。
- **未覆盖检查项的原因以 `stats.skipped_rules[].reason` 为准**，不要凭产品类型推测。规则可能因产品类型、受众范围、材料载体、机构类型中任一维度被跳过，原因字段已给出准确结论。

### 第五步：批量重扫（可选）

新规发布后需要重扫存量材料时，对目录下所有材料逐份扫描并汇总。重点关注 `status` 为 `pending` 的规则命中情况，这些是新规生效后会变成违规的内容。

```bash
for f in <目录>/*.md; do
  python3 {skill_dir}/scripts/scan_rules.py --file "$f" --product public_fund --json
done
```

汇总时按规则维度聚合，指出哪些材料受同一条新规影响，便于统一整改。

## 规则库

| 文件 | 内容 |
|---|---|
| `references/rules/banned-expressions.json` | 不该出现的表述 |
| `references/rules/performance-display.json` | 业绩数据展示方式 |
| `references/rules/mandatory-elements.json` | 必须出现的要素与法定声明 |
| `references/rules/format-presentation.json` | 排版与呈现形式 |
| `references/rules/SCHEMA.md` | 规则编写规范 |
| `references/legal-index.md` | 法条索引，含条款原文、核实状态、时效警示 |

用户要求新增或修改规则时，先读 `SCHEMA.md`，改完运行校验：

```bash
python3 {skill_dir}/scripts/validate_rules.py
```

校验会检查字段合法性、正则可编译性、法条引用是否存在，并运行每条规则的内嵌测试样例。**校验不通过不得交付。**

两条硬约束，校验脚本会强制执行：

- 行业惯例项（`basis` 为 `industry_practice`）不得挂条款号
- 无法条依据的规则不得判定为违规

## 时效性提醒

规则库中法规状态截至 `references/legal-index.md` 标注的核实时间。以下两点在每次审查时需注意：

- 《金融产品网络营销管理办法》2026 年 9 月 30 日施行，相关规则当前标记为 `pending`，报告中单独分节
- 证监会公告〔2008〕2 号已被废止，其禁用词（净值归一、首只、最大、最好、最强）在公募场景无现行条款依据，规则库中标记为行业惯例。若用户或其他资料坚持引用该文件，应说明其已废止

若用户提及规则库未收录的新规，如实说明未收录，不要凭印象引用条款号。
